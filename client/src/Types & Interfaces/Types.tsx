export type Message = {
  author: string;
  date: number;
  message: string;
  messageId: string;
  name: string;
};

export type messageList = {
  author: string;
  date: number;
  message: string;
  messageId: string;
  name: string;
};
